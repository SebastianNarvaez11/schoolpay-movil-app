import { START_LOADING, FINISH_LOADING } from '../actions/authActions'

const initialState = {
    
}


const uiReducer = (state = initialState, action) => {
    switch (action.type) {

        

        default:
            return state
    }
}

export default uiReducer